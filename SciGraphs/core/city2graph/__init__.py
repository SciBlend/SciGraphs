"""city2graph wrappers that materialize their results as Blender objects.

The Blender-free half is `scigraphs_core.city2graph`. See `SciGraphs.core` for
the split and why this package has no lazy table.
"""
